#' simple_sums is a load of nonsense
#'
#' x is multiplied by 17.4
#' @param x is what you want to multiply by 17.4
#' @return 17.4 times what you put in!
#' @examples
#' ttrrt <- simple_sums(x = 1);
#' df2 <- simple_sums(x = 12);
#' @export

simple_sums <- function(x) {
  y <- x*17.4
  return(y)
}
